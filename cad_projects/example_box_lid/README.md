# Example box-and-lid project

This is a generic starter project for the Fusion add-in workflow.

It is intentionally simple:

- `Example_Box` creates a rectangular base
- `Example_Lid` creates a matching lid
- `assembly.py` positions the lid above the box for preview

Use this project as a template for new designs, not as a final production part.
